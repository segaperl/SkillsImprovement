import { MobileClickOutsideDirective } from './mobile-click-outside.directive';

describe('MobileClickOutsideDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileClickOutsideDirective();
    expect(directive).toBeTruthy();
  });
});
